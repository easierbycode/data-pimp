# Local sample-price store written by the migrated dashboard backend
# (core/samples.ts). Ephemeral on Deno Deploy; never commit it.
.thirsty/
.env

# Compiled desktop binaries (build:mac / build:windows) — large, never commit.
build/
.DS_Store

# Vendored Fresh member app (member/): node_modules is local-only.
# NOTE: member/_fresh (the built Fresh server) IS committed on purpose — Deno
# Deploy serves data-pimp without a build step, so the built member app must be
# in the repo for /member to work in production. Rebuild + commit it after any
# member/ change with `deno task member:build`.
member/node_modules/
node_modules/
