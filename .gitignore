# Local sample-price store written by the migrated dashboard backend
# (core/samples.ts). Ephemeral on Deno Deploy; never commit it.
.thirsty/
.env

# Compiled desktop binaries (build:mac / build:windows) — large, never commit.
build/
.DS_Store

# Vendored Fresh member app (member/): build output + deps. Regenerated by
# `deno task member:build`; embedded into the binary via --include member/_fresh.
member/_fresh/
member/node_modules/
node_modules/
